<!DOCTYPE html>
<html lang="en">
	<%- include('partials/head',{title: "producto"});%>

<body>
	<%- include('partials/header');%>

	<div class="container">
		<div class="row">
			<div class="col-12">
				<nav aria-label="breadcrumb">
					<ol class="breadcrumb">
						<li class="breadcrumb-item"><a href="#">Home</a></li>
						<li class="breadcrumb-item"><a href="#">Library</a></li>
						<li class="breadcrumb-item active" aria-current="page"><%= `${product.id_category}` %></li>
					</ol>
				</nav>
			</div>
		</div>
	</div>
	<div class="container">
		<div class="row">
			<div class="col-6">
				<img src="/images/productImages/<%= product.image %>" class="img-fluid" alt="Responsive image">		
			</div>
			<div class="col-6">
				<div class="row">
					<div class="col-12">
						<h2><%= product.product_name %></h2>
					</div>
				</div>
				<div class="row">
					<div class="col-6">
						<ul>
							<li><%= product.brand %></li>
							<li><%= product.model %></li>
							<li><%= product.color %></li>
						</ul>
					</div>
					<div class="col-6">
						<% if(product.sale_price != null){ %>
							<h4><strike>$ <%= product.list_price %></strike></h4>
							<div class="col-12 d-flex justify-content-start" style="padding-left: 0px;">
								<h4 style="color: red; padding-right: 3px;">¡Oferta!:</h4> 
								<h4>$<%= product.sale_price %></h4>
							</div>
						<%}else{%>
							<h4>$ <%= product.list_price %></h4>
							<%}%>

						<p>Hasta 12 cuotas sin interes</p>
						<p>Recibilo en 2 dias</p>
						<p>Tu compra ayuda al medioambiente</p>
					</div>
				</div>
				<div class="row">
					<div class="col-12 d-flex justify-content-end">
						<button type="button" class="btn btn-secondary m-2">Agregar al Carrito</button>
						<button type="button" class="btn btn-primary m-2">Comprar</button>
						<form action="/productDetail/<%= product.id %>/borrar?_method=DELETE" method="POST" ><button type="button" class="btn btn-primary m-2">Borrar</button></form>
					</div>
				</div>
			</div>	
		</div>
	</div>

	<%- include('partials/footer');%>
	<%- include('partials/scripts');%>

</body>
</html>